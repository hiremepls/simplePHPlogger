# simplePHPlogger

source system must have lm-sensors package installed

visualizer and logmaker must be in the same folder (or you must edit the paths accordingly)

bash script runs on a machine you want to track(edit accordingly)
